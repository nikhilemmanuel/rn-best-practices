import React from 'react';
import App from './App';
import Sum from './Sum'

import renderer from 'react-test-renderer';

it('Sum of 2 values is', () => {
  // const rendered = renderer.create(<App />).toJSON();
  // expect(rendered).toBeTruthy();


});
